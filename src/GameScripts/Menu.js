import React from "react";
import { useSpring, animated } from "react-spring";

export default function Menu(props) {
  const page = <div className="Menu-background"></div>;
  console.log("called menu");
  return page;
}
